services:
  java:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: container-java
    working_dir: /workspace
    volumes:
      - ./src:/workspace/src
      - ./out:/workspace/out
    stdin_open: true
    tty: true
